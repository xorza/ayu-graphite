# Open issues

- `claude/build.py`: `orange_FOR_SUBAGENTS_ONLY` (syn_keyword) and `pink_FOR_SUBAGENTS_ONLY` (syn_operator) resolve to the same cell, so two of the eight subagent colours are identical.
